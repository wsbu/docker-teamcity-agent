TeamCity Build Agent in Docker
==============================

With inspiration from JetBrain's own Docker image for TeamCity agents, this
image builds off of our "toolchain-native" image and adds a TeamCity build agent
to the mix.
